name: get-pr
on:
  pull_request:
    types: [opened, synchronize, reopened, edited, labeled, unlabeled, closed]
    branches:
      - main
  issue_comment:
    types: [edited]

env:
  CI: true
  NODE_OPTIONS: "--max-old-space-size=4096"

concurrency:
  group: ${{ github.event.number || github.event.issue.number }}

jobs:
  version:
    permissions: write-all
    name: static checks
    runs-on: ubuntu-22.04
    steps:
      # - uses: actions/checkout@v3
      # - run: mkdir -p artifact-path
      # - run: echo ${{ github.event.number || github.event.issue.number }} > artifact-path/pr.txt
      # - uses: actions/upload-artifact@v3
      #   with:
      #     name: pr
      #     path: echo hello > artifact-path/pr.txt
      - run: echo "hello"
